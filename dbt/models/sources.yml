version: 2

sources:
  - name: raw
    description: Raw Superstore CSV data loaded via dbt seed
    schema: main
    tables:
      - name: raw_superstore
        description: "Sample Superstore dataset — 9,994 retail transactions (2014-2017)"
